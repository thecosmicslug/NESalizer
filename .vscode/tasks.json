{
  "version": "2.0.0",
  "tasks": [{
      "label": "build-nesalizer",
      "command": "", // Could be any other shell command
      "args": ["${workspaceFolder}/build_nesalizer_steamlink.sh"],
      "type": "shell"
  }]
}